services:
  - type: web
    name: instagram-feed-generator-api
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3001
      - key: CORS_ORIGIN
        sync: false
